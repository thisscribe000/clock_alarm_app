name: calc_convert
description: A beautiful calculator and currency converter
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.1.1
  
  # Storage
  shared_preferences: ^2.2.2
  
  # API
  http: ^1.1.0
  
  # Haptic Feedback
  vibration: ^3.1.7
  
  # Share
  share_plus: ^9.0.0
  
  # Widget
  # home_widget: removed temporarily while disabling widgets
  
  # Icons
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true